---
interface Props {
    title: string;
    subtitle?: string;
    /** Optional eyebrow label above the title (e.g. "Series") */
    eyebrow?: string;
    /** Optional screen-reader-only suffix appended to the h1 for SEO H1 length */
    srOnlySuffix?: string;
}

const { title, subtitle, eyebrow, srOnlySuffix } = Astro.props;
---

<header class="mb-16 text-center">
    {eyebrow && (
        <p class="text-sm font-semibold uppercase tracking-wide text-muted-foreground mb-3">
            {eyebrow}
        </p>
    )}
    <h1 class="text-4xl font-bold tracking-tight text-foreground sm:text-5xl text-balance">
        {title}{srOnlySuffix && <span class="sr-only">{srOnlySuffix}</span>}
    </h1>
    {subtitle && (
        <p class="mt-4 text-lg text-muted-foreground">{subtitle}</p>
    )}
</header>
